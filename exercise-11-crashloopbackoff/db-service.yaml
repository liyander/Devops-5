apiVersion: v1
kind: Service
metadata:
  name: payment-db
  namespace: production
spec:
  selector:
    app: payment-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
